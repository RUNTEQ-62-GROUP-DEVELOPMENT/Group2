<h3>マイページ</h3>
<p><%= current_user.name %>さん、こんにちは</p>

<!-- 読書中の本一覧 -->
<h4>読書中の本</h4>

<!-- 読書中の本一覧表-->
<table class="table">
  <tr>
    <th>タイトル</th>
    <th>著者</th>
    <th>総ページ数</th>
  </tr>
  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
        <td><%= book.author %></td>
        <td><%= book.pages %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<!-- 挑戦中の目標一覧 -->
<h4>挑戦中の目標</h4>
<table class="table">
  <tr>
    <th>タイトル</th>
    <th>読書開始日</th>
    <th>読了目標日</th>
    <th>目標ペース<br>（ページ/日）</th>
    <th>進捗</th>
  </tr>
  <tbody>
    <% @goals.each do |goal| %>
      <tr>
        <td><%= goal.title %></td>
        <td><%= goal.start_date %></td>
        <td><%= goal.target_date %></td>
        <td><%= goal.pages_per_day %></td>
        <%# *を%に変える <td>"<*= goal.current_page *>/ <*= book.pages *><br><*= @progress *> %"</td>%>
      </tr>
    <% end %>
  </tbody>
</table>